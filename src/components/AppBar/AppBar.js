import Navigation from "../Navigation/Navigation";

const AppBar = () => {
  return (
    <header>
      <div className={"container"}>
        <Navigation />
      </div>
    </header>
  );
};

export default AppBar;
